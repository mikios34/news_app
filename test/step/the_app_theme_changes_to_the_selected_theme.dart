import 'package:flutter_test/flutter_test.dart';

/// Usage: the app theme changes to the selected theme
Future<void> theAppThemeChangesToTheSelectedTheme(WidgetTester tester) async {
  await Future.delayed(const Duration(seconds: 5));
}
